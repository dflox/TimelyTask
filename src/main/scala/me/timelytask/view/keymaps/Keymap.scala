package me.timelytask.view.keymaps

import me.timelytask.model.settings.ViewType
import me.timelytask.util.Publisher

class Keymap(using activeViewPublisher: Publisher[ViewType]) {
  
  //def handle(Key k)
}
